- @body_id = 'contact_us'
%div{:style => "width: 380px; float: left; display: block;"}
  #contact-us
    %h1= t("contact_us")
    = hook :contact do
      = render "shared/error_messages", :target => @contact
      = form_for(@contact, :html => {:honeypot => true}) do |f|
        = hook :contact_inside_form do
          .field
            = f.label :name, t("name")
            %br/
            = f.text_field :name
          .field
            = f.label :email, t("email")
            %br/
            = f.text_field :email
          .field
            = f.label :order_number, "Transaction Number"
            (#{t("optionnal")})
            %br/
            = f.text_field :order_number
          %br/
          .field
            = f.label :topic_id, t("topic")
            = f.collection_select :topic_id, @topics, :id, :name
          .field
            = f.label :message, t("message")
            %br/
            = f.text_area :message
          .action= f.submit "Punch it Chewy", :class => "button primary", :style => "margin-top: 10px;"
%div{:style => "float: left; width: 400px; display: block;"}
  %h3{:style => "font-family: 'Rock Salt'; margin-top: 46px; line-height: 25px;"}
    To write to the True Jersey staff simply fill out the form below and tell Chewy to punch it. If you are writing in regard to an order please include the full name, shipping address, and transaction number so we can quickly get back to you! 